nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
sudo nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
sudo nmcli con mod enp0s3 ipv4.method manual
sudo nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed
-------------------------------------------------------------
nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
sudo nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
sudo nmcli con mod enp0s3 ipv4.method manual
sudo nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed
-------------------------------------------------------------
nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed
-------------------------------------------------------------
nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
sudo nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
sudo nmcli con mod enp0s3 ipv4.method manual
sudo nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed
-------------------------------------------------------------
nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
sudo nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
sudo nmcli con mod enp0s3 ipv4.method manual
sudo nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed
-------------------------------------------------------------
nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
sudo nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
sudo nmcli con mod enp0s3 ipv4.method manual
sudo nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed
-------------------------------------------------------------
nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
sudo nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
sudo nmcli con mod enp0s3 ipv4.method manual
sudo nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed
-------------------------------------------------------------
nmcli connection show

sudo nmcli con mod enp0s3 ipv4.addresses xxx.xx.x.xx/24  --> Buraya local ip gelecek
sudo nmcli con mod enp0s3 ipv4.gateway xxx.xx.x.xx --> Buraya local ip gateway gelecek
sudo nmcli con mod enp0s3 ipv4.dns "8.8.8.8 1.1.1.1"
sudo nmcli con mod enp0s3 ipv4.method manual
sudo nmcli con up enp0s3

ip a
ping 192.168.1.1
ping 8.8.8.8

hostnamectl status
hostnamectl set-hostname YENI_HOSTNAME
hostnamectl
systemctl restart systemd-hostnamed

